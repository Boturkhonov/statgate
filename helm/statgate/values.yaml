replicaCount: 1

image:
  repository: statgate-controller
  tag: latest
  pullPolicy: IfNotPresent

serviceAccount:
  name: statgate-controller

resources:
  limits:
    cpu: 200m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 64Mi

leaderElect: true
